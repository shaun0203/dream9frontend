name: dream9frontend
description: Flutter frontend for dream analysis app

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  http: ^1.1.0
  flutter_tts: ^3.8.0
  equatable: ^2.0.5

flutter:
  uses-material-design: true
